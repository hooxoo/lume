# Auto-generated from palette.json — do not edit manually
# Run `bun run generate` to regenerate
#
# Lume — tmux theme
# https://github.com/danfry1/lume

set -ga terminal-overrides ",*:RGB"

set -g status-style "bg={{backgrounds.mantle}},fg={{foregrounds.subtext}}"
set -g status-left-length 40
set -g status-right-length 40
set -g status-left "#{?client_prefix,#[fg={{backgrounds.crust}}#,bg={{accents.rose}}#,bold] PREFIX ,#[fg={{backgrounds.crust}},bg={{accents.lavender}},bold] #{session_name} }#[fg=default,bg={{backgrounds.mantle}},nobold] "
set -g status-right "#[fg={{foregrounds.overlay}}] %H:%M #[fg={{foregrounds.overlay}}]│ #[fg={{foregrounds.overlay}}]#{host_short} "

set -g window-status-format "#[fg={{foregrounds.overlay}},bg={{backgrounds.mantle}}] #{window_index}:#{window_name} "
set -g window-status-current-format "#[fg={{foregrounds.text}},bg={{backgrounds.surface0}},bold] #{window_index}:#{window_name} "
set -g window-status-separator ""

set -g pane-border-style "fg={{backgrounds.surface1}}"
set -g pane-active-border-style "fg={{accents.lavender}}"

set -g mode-style "fg={{backgrounds.crust}},bg={{accents.lavender}}"

set -g message-style "fg={{foregrounds.text}},bg={{backgrounds.surface0}}"
set -g message-command-style "fg={{foregrounds.text}},bg={{backgrounds.surface0}}"

set -g clock-mode-colour "{{accents.lavender}}"

set -g copy-mode-match-style "fg={{backgrounds.crust}},bg={{accents.honey}}"
set -g copy-mode-current-match-style "fg={{backgrounds.crust}},bg={{accents.peach}}"
